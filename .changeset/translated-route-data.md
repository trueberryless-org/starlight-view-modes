---
"starlight-view-modes": patch
---

Fixes the `title` and `switchToText` properties of the [view modes data](https://starlight-view-modes.netlify.app/view-modes-data/) being UI string keys instead of translated strings.
