---
"starlight-view-modes": patch
---

Removes the `is-absolute-url`, `pagefind`, `rehype-parse`, `rehype-stringify`, and `unified` dependencies and refactors internal logic to small pure functions for improved maintainability.
