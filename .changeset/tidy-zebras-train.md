---
"starlight-view-modes": patch
---

The minimum supported version of Starlight is now version `0.32.6` (surpassed by CSS layers changes anyway a.k.a. `0.34.0`) because internals got refectored to use the [new simplified `isFallback` passing type](https://github.com/withastro/starlight/pull/3030).
