---
import { prefixInternalLinks } from "../../libs/html";
import { ZenMode } from "../../libs/modes";

const html = prefixInternalLinks(await Astro.slots.render("default"), ZenMode);
---

<Fragment set:html={html} />
